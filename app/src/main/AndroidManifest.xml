<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashish060792.funwithkotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginTheme">
        <activity android:name=".ui.activity.sampleActivities.MainActivity" />
        <activity android:name=".ui.activity.github.GitHubSearchUserActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activity.sampleActivities.RangeActivity" />
        <activity android:name=".ui.activity.github.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.sampleActivities.OperatorOverloading" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service android:name=".ui.activity.notification.ShowNotificationService" />
    </application>

</manifest>